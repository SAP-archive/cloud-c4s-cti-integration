# SAP Service Cloud/ Sales Cloud CTI Widget Example 

This project provides a sample implementation for a widget to push information to C4C Application

# Running the Widget Sample

npm install

npm start

# Configuration in SAP cloud for Service
In the live activity configuration screen
Select 
- Provider as "External Provider"
- Provider url as "http://localhost:3000" ( npm default port is 3000 )

